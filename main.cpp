#include <iostream>
#include "lista.h"
#include "pilha.h"
#include "fila.h"
using namespace std;

int main(){
    /*
        cout << "TESTE PILHA" << endl;
        Pilha* pilha = criaPilha();
        pilha = push(pilha, 5);
        pilha = push(pilha, 9);
        pilha = push(pilha, 2);
        pilha = push(pilha, 1);

        //cout << removerRepetidosPilha(pilha) << endl;
        //printPilhaControle(pilha);

        //cout << removerParesPilha(pilha) << endl;
        //printPilhaControle(pilha);

        //cout << buscaRemoverPilha(pilha, 0) << endl;
        //printPilhaControle(pilha);

        //cout << buscaElementoPilha(pilha, 1) << endl;
        //printPilhaControle(pilha);
    */


    /*
        cout << "TESTE LISTA" << endl;
        Node* lista1 = criaLista();
        lista1 = insereIniLista(lista1, 3);
        lista1 = insereIniLista(lista1, 5);
        lista1 = insereIniLista(lista1, 10);
        lista1 = insereIniLista(lista1, 9);

        Node* lista2 = criaLista();
        lista2 = insereIniLista(lista2, 3);
        lista2 = insereIniLista(lista2, 5);

        //cout << isSubLista(lista1, lista2);

        //lista1 = reverseLista(lista1);
        //printLista(lista1);

        //lista1 = appendLista(lista1, lista2);
        //printLista(lista1);

        //cout << lenLista(lista);
        //cout << mediaLista(lista);
        //cout << maxLista(lista);
        //cout << minLista(lista);
    */

    /*
        Teste de fila não tem pq apaguei e não salvei em lugar nenhum.
    */

    return 0;
}
